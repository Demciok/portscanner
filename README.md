# keylogger
Simple port scanner using the socket module to send TCP packet and concurrent.futures to speed up the program.

# How to use it ? 
Just run in your python compiler.

# What that do ? 
Scanning ports range from 1 to 1024, and printing open ports with the banner ( the service running on this port ).
